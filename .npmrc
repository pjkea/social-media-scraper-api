# Suppress deprecated package warnings
audit-level=moderate

# Use exact versions for better stability
save-exact=true

# Faster installs
prefer-offline=true

# Security settings
fund=false

# Puppeteer settings
puppeteer_skip_chromium_download=false
puppeteer_download_host=https://storage.googleapis.com

# Progress settings
progress=true
loglevel=warn